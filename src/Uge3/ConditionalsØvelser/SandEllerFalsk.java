package Uge3.ConditionalsØvelser;

public class SandEllerFalsk {
    // CONDITIONALS - ØVELSE 1
    //int x = 27;
    //int y = -1;
    //int z = 32;
    //boolean b = false;
    //Hvad er værdien af følgende udtryk?

// !b                       svar: true
// b || true                svar: true
// (x > y) && (y > z)       svar:
// (x == y) || (x <= z)     svar:
// !(x % 2 == 0)            svar:
// (x % 2 != 0) && b        svar:
// b && !b                  svar:
// b || !b                  svar:
// (x < y) == b             svar:
}
